name: ✨ trioffice CD ✨

on:
  push:
    branches:
      - 'develop'

jobs:
  backend-docker-build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: ✨ Checkout repository
        uses: actions/checkout@v3
        with:
          ref: refs/heads/develop

      - name: application.yml 생성
        run: |
          echo "${{ secrets.APPLICATION_YML }}" > src/main/resources/application.yml

      - name: Set up Json file
        env:
          JSON_SECRET: ${{ secrets.FIREBASE_PRIVATE_KEY_JSON }}
          JSON_DIR: src/main/resources/static
          JSON_FILE_NAME: firebase-key.json
        run: |
          echo "${JSON_SECRET}" > $JSON_DIR/$JSON_FILE_NAME

      - name: Set up firebase js
        env:
          FIREBASE_JS: ${{ secrets.FIREBASE_JS }}
          FIREBASE_JS_DIR: src/main/resources/static
          FIREBASE_JS_FILE_NAME: firebase-messaging-sw.js
        run: |
          echo "${FIREBASE_JS}" > $FIREBASE_JS_DIR/$FIREBASE_JS_FILE_NAME

      - name: ✨ JDK 17 설정
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: ✨ Maven Caching
        uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: ✨ Jar 파일 빌드
        run: mvn clean package

      - name: ✨ DockerHub에 로그인
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_PASSWORD }}

      - name: ✨ Docker Image 빌드 후 DockerHub에 Push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ secrets.DOCKER_REPO }}/trioffice:latest

  backend-docker-pull-and-run:
    runs-on: [ self-hosted, prod ]
    needs: [ backend-docker-build-and-push ]
    if: ${{ needs.backend-docker-build-and-push.result == 'success' }}
    steps:
      - name: ✨ 배포 스크립트 실행
        run: sh /home/ubuntu/deploy.sh